# Postmortem: JSON 解析失败导致工具链路中断

## 现象

模型输出包含噪声文本与非法 JSON（单引号、尾逗号），导致反序列化失败。

## 根因

- 模型未严格遵循 JSON-only 输出。
- 上层没有自动修复与重试。

## 修复

1. 引入 `json_guard.parse_with_guard`。
2. 修复器执行：提取 JSON 主体、单引号替换、去尾逗号。
3. 修复后再次 schema 校验。

## 结果

- 对常见格式错误具备自愈能力。
- 无法修复时返回标准错误码 `JSON_PARSE_FAILED`，可被评估与告警系统识别。
